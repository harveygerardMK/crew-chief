---
import { raceBroadcastUpdates } from "../lib/data";
import { hasBroadcastUpdates } from "../lib/broadcast";

const updates = hasBroadcastUpdates(raceBroadcastUpdates) ? raceBroadcastUpdates : [];

function formatUpdatedAt(iso: string): string {
  return new Date(iso).toLocaleString("en-US", {
    timeZone: "America/Los_Angeles",
    weekday: "short",
    month: "short",
    day: "numeric",
    hour: "numeric",
    minute: "2-digit",
    timeZoneName: "short",
  });
}

function previewDoing(doing: string | null): string {
  const text = doing?.trim();
  if (!text) return "Update posted";
  return text.length > 72 ? `${text.slice(0, 72)}…` : text;
}

function displayValue(value: string | null | undefined): string {
  const text = value?.trim();
  return text || "—";
}
---

<section class="live-updates" aria-labelledby="live-updates-heading">
  <header class="live-updates__header">
    <h2 id="live-updates-heading" class="live-updates__title">
      Live Race Updates from Harvey's Cuter Half
    </h2>
    {
      updates.length > 0 && updates[0]?.updated_at && (
        <p class="live-updates__meta">
          Latest {formatUpdatedAt(updates[0].updated_at)}
          {updates.length > 1 && ` · ${updates.length} updates`}
        </p>
      )
    }
  </header>

  {
    updates.length > 0 ? (
      <div class="live-updates__list">
        {
          updates.map((entry, index) => {
            const number = index + 1;
            const openDefault = index === 0;
            return (
              <details class="live-update-item" open={openDefault}>
                <summary class="live-update-item__summary">
                  <span class="live-update-item__title">Update #{number}</span>
                  <span class="live-update-item__preview">
                    {previewDoing(entry.doing)}
                    {entry.updated_at && (
                      <span class="live-update-item__when">
                        {formatUpdatedAt(entry.updated_at)}
                      </span>
                    )}
                  </span>
                </summary>

                <div class="live-update-item__panel">
                  <div class="live-update-field">
                    <p class="update-field__label">How's Harvey doing?</p>
                    <div class="live-update-field__value">{displayValue(entry.doing)}</div>
                  </div>

                  <div class="live-update-field">
                    <p class="update-field__label">Last seen aid station</p>
                    <div class="live-update-field__value">
                      {displayValue(entry.last_seen?.station ?? null)}
                    </div>
                  </div>

                  <div class="live-update-field">
                    <p class="update-field__label">Time last seen</p>
                    <div class="live-update-field__value">
                      {displayValue(entry.last_seen?.time_label ?? null)}
                    </div>
                  </div>

                  <div class="live-update-field">
                    <p class="update-field__label">Note for family (optional)</p>
                    <div class="live-update-field__value live-update-field__value--note">
                      {displayValue(entry.note)}
                    </div>
                  </div>

                  {
                    entry.photos.length > 0 && (
                      <div class="live-update-field">
                        <p class="update-field__label">Photos</p>
                        <div
                          class="live-updates__photos"
                          role="group"
                          aria-label="Photos from crew"
                        >
                          {
                            entry.photos.map((photo) => (
                              <a
                                class="live-updates__photo-link"
                                href={photo.url}
                                target="_blank"
                                rel="noopener noreferrer"
                              >
                                <img
                                  src={photo.url}
                                  alt={photo.alt}
                                  loading="lazy"
                                  width="160"
                                  height="160"
                                />
                              </a>
                            ))
                          }
                        </div>
                      </div>
                    )
                  }
                </div>
              </details>
            );
          })
        }
      </div>
    ) : (
      <p class="live-updates__empty">
        Updates will show up here during race weekend when Karen posts from the trail.
        Check back after the start on Friday morning.
      </p>
    )
  }
</section>
