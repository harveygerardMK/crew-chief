name: Agent health check (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Crew Chief is shut down
        run: echo "Race is over; live Crew Chief smoke checks are disabled to avoid token spend."
